{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome against localhost",
            "type": "pwa-chrome",
            "request": "launch",
            "url": "http://localhost:3000", // Adjust the URL to match your development server
            "webRoot": "${workspaceFolder}/src"
        }
    ]
}
